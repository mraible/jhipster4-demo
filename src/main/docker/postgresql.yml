version: '2'
services:
    blog-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/blog/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=blog
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
